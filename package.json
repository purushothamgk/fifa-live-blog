{
  "name": "fifa-live-blog",
  "version": "1.0.0",
  "private": true,
  "description": "Live FIFA match timelines in a focused web UI",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=18"
  }
}
